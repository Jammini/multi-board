name: board-batch-server-build-and-push

on:
  push:
    branches:
      - main
    paths:
      - 'board-batch-server/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./board-batch-server/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-batch-server:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-batch-server:${{ github.sha }}
          platforms: linux/amd64,linux/arm64

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Pull latest image
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/board-batch-server:latest

            # Stop and remove existing container
            docker stop board-batch-server || true
            docker rm board-batch-server || true

            # Run new container
            docker run -d \
              --name board-batch-server \
              --network app-network \
              -e SPRING_PROFILES_ACTIVE=local \
              --restart unless-stopped \
              ${{ secrets.DOCKERHUB_USERNAME }}/board-batch-server:latest

            # Clean up old images
            docker image prune -f
