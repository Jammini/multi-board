name: board-admin-client-build-and-push

on:
  push:
    branches:
      - multi-board-67
    paths:
      - 'board-admin-client/**'

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./board-admin-client/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/board-admin-client:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/board-admin-client:${{ github.sha }}
          platforms: linux/amd64,linux/arm64

      - name: Deploy to EC2
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Pull latest image
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/board-admin-client:latest

            # Stop and remove existing container
            docker stop board-admin-client || true
            docker rm board-admin-client || true

            # Run new container
            docker run -d \
              --name board-admin-client \
              --network app-network \
              -p 81:80 \
              --restart unless-stopped \
              ${{ secrets.DOCKERHUB_USERNAME }}/board-admin-client:latest

            # Clean up old images
            docker image prune -f
